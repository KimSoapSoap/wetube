doctype html 
html(lang="ko")
    head 
        title #{pageTitle} | Wetube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        block header        
        header
            h1=pageTitle
            nav
                ul
                    li 
                        a(href="/") &larr; Home 
                    if loggedIn
                        li
                            a(href="/user-profile") #{loggedInUser.name}
                    li 
                        a(href="/videos/upload") upload video &uarr;            
                    li
                        a(href="/search") Search
                    if !loggedIn
                        li
                            a(href="/join") Join
                        li 
                            a(href="/login") Login   
                    else 
                        li 
                            a(href="/logout") Log Out   

            block header2                     
        main            
            block content 
    include partials/footer