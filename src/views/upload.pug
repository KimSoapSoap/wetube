extends base.pug

block content 
    if errorMessage
        span=errorMessage 
    form(method="POST")
        input(name="title" type="text" required placeholder="Video Title")
        input(name="description" type="text" required placeholder="Description")
        input(name="hashtags" type="text" required  placeholder="Hashtags(add with comma)")
        input(type="submit" valuse="Upload")
    